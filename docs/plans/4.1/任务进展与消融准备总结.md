- 我这边中期目标已经完成，主流程代码和汇报材料都形成了，现阶段主要是后续全量实验+写论文.
- 我会按“四数据集基线 -> 四策略对比 -> 消融四组 -> 汇总回填论文”的顺序推进。 
## 3. 已跑过的数据集与结果覆盖
**300 组**
- 数据集覆盖（按 `dataset` 字段）：
  - `hypo`：100 组
  - `hypo-l`：100 组
  - `lcc`：50 组
  - `trofi`：50 组
---

## 4. 消融实验准备与操作进度

### 4.1 已定义的消融分组

- `wo_feature`：关闭情感词典 + 概念特征
- `plus_e`：仅保留情感特征
- `plus_c`：仅保留概念特征
- `full`：特征 + 级联全开
- `wo_cascade`：保留特征，关闭 Stage-2 深度级联增强

### 4.2 已完成的执行动作

- 已完成一轮**小规模先导性消融验证**（用于验证流程可跑通，不作为最终结论）：
  - 依托历史结果目录完成了分组口径映射与指标回放（如 `bench10` / `strat30` / `current_fix` 系列）
  - 覆盖数据集：`hypo`、`hypo-l`、`lcc`、`trofi`
  - 样本规模：以 5/10/30 条为主，主要用于检查模块开关、指标统计与结果落盘链路
  - 当前结论定位：可证明“消融流程已具备执行能力”
- 已生成消融任务矩阵计划：
  - 任务规模：**20 组（4 数据集 × 5 配置）**
